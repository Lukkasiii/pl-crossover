__pycache__/
*.py[cod]
.venv/
venv/
data/raw/
# data/pl.db is committed (<1MB, built+validated) so CI and the demo export
# never depend on a live Understat fetch -- see README.
# .e2e-pl.db is a disposable copy of it (web/playwright.config.ts) so the
# auth e2e spec's writes never touch the tracked file.
data/.e2e-pl.db
node_modules/
dist/
.env
.DS_Store

# scratch output from fetch/TLS debugging (scripts/diagnose_fetch.py, diagnose_tls.py)
*.log
page.html

# Claude Code scratch: task files written by the planning session, not project history
.claude/commands/next.md

# Private interview prep, not part of the portfolio the repo presents
docs/interview-notes.md
