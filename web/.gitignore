# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# regenerate with `npm run generate:types`
openapi.json

# regenerate with `npm run demo:data` -- frozen from data/pl.db, ~16MB, not worth
# carrying in git history when the CI/deploy job can rebuild it in under a second
public/demo/*.json

# Playwright
/test-results/
/playwright-report/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
