import { PlaceholderPage } from "./PlaceholderPage";

export default function Model() {
  return <PlaceholderPage titleKey="placeholder.model.title" bodyKey="placeholder.model.body" testid="page-model" />;
}
